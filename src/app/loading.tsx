import LoadingSpinner from '../components/helper/loading-spinner';

export default function Loading() {
	return (
		<section style={{ margin: '1.5rem auto' }}>
			<LoadingSpinner />
		</section>
	);
}
